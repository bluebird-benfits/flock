/**
 *   Profiles Controller
 */

export async function buildEmployeeProfile(employeeStub) {
    // Function is passed a stub of an employee record from the /directory response saved to Mongo
    let profile = employeeStub
    // 1. get the pay statement
    // 2. get the pass-through data
    // 3. sum the various 
}